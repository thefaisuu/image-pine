export const metadata = {
  title: "Image Pine - Vectorizer & Pixel Art to SVG Converter",
  description: "Convert low-resolution logos, pixel art, or drawings into high-quality infinite-resolution SVGs locally in your browser.",
};

export default function VectorizerLayout({ children }) {
  return <>{children}</>;
}
