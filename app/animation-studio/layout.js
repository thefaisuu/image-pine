export const metadata = {
  title: "Image Pine - APNG & Animated WebP Animation Studio",
  description: "Compile frame sequences into high-quality APNG or WebP animations, or split existing files into raw frames locally.",
};

export default function AnimationStudioLayout({ children }) {
  return <>{children}</>;
}
