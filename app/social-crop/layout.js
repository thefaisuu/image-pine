export const metadata = {
  title: "Image Pine - Social Media Cropper & Live Mockup Previewer",
  description: "Batch crop images to standard ratios for Instagram, YouTube, Twitter, and LinkedIn. Preview crops inside realistic mobile feeds and download as a ZIP.",
};

export default function SocialCropLayout({ children }) {
  return <>{children}</>;
}
