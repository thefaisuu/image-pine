export const metadata = {
  title: "Image Pine - QR Code Generator & Branding Studio",
  description: "Generate customized QR codes with brand colors and center logo overlays. Highly scannable, safe, and operates entirely in the browser.",
};

export default function QrGeneratorLayout({ children }) {
  return <>{children}</>;
}
