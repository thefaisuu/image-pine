export const metadata = {
  title: "Image Pine - SVG Editor & Path Optimizer",
  description: "Minify SVG vector graphics. Remove namespaces, strip metadata, reduce curves decimal coordinate precision, and preview size savings.",
};

export default function SvgOptimizerLayout({ children }) {
  return <>{children}</>;
}
