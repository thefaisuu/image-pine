export const metadata = {
  title: "Image Pine - Canvas Drawing & Annotation Markup",
  description: "Draw shapes, write with brushes, highlight areas, or erase custom annotations directly on mockups and screenshots locally in your browser.",
};

export default function AnnotateLayout({ children }) {
  return <>{children}</>;
}
