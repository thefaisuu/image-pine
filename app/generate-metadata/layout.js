export const metadata = {
  title: "Generate Image Metadata Online for Free - ImagePine",
  description: "Automatically generate titles and keywords for your images using xAI's Grok Vision API. Configure custom constraints, track progress, and download a CSV of the results.",
};

export default function GenerateMetadataLayout({ children }) {
  return <>{children}</>;
}
