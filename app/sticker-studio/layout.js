export const metadata = {
  title: "Image Pine - Face Mask & Sticker Privacy Editor",
  description: "Doodle, apply privacy mask emojis, censorship grids, and stickers onto photos locally in your browser before sharing.",
};

export default function StickerStudioLayout({ children }) {
  return <>{children}</>;
}
