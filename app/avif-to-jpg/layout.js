export const metadata = {
  title: "Convert AVIF to JPG Online for Free | ImagePine",
  description: "Convert modern AVIF images to standard JPG format for compatibility with older devices. Fast, free, and 100% private.",
};

export default function AvifToJpgLayout({ children }) {
  return <>{children}</>;
}
