export const metadata = {
  title: "ImagePine - Edit PDF Documents Online",
  description: "Free client-side PDF Editor. Reorder pages, rotate, delete, insert blank pages, and add text annotations directly in your browser.",
};

export default function PdfEditorLayout({ children }) {
  return <>{children}</>;
}
