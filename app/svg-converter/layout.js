export const metadata = {
  title: "Convert SVG to PNG & JPG Online for Free - ImagePine",
  description: "Convert SVG vector graphics to raster formats like PNG or JPG, or convert raster to vector SVG.",
};

export default function SvgConverterLayout({ children }) {
  return <>{children}</>;
}
